/**
 * Indicates the current semester inorder to separate Firebase logs from each other
 * @type {string}
 */
const CURRENT_SEMESTER = "Spring 2022"

const SESSION_SYSTEM = "SEMESTER"

module.exports = {
    CURRENT_SEMESTER,
    SESSION_SYSTEM
}
