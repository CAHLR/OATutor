import steps from './slope3-index.js'

const problem = {
  id: 'slope3', //Substeps will be in the form problem.id + 'a' and so on
  title: "Slope of a line",
  body: "Which statement best describes the slope of the line graphed in the diagram below?",
  steps: steps
};
export { problem };