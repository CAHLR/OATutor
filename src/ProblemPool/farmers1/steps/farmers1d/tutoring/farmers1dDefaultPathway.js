var hints = [{id: "farmers1d-h1", type: "hint", dependencies: [], title: "drop function", text: "drop serves the same purpose as select, but it takes away the columns that you provide rather than the ones that you don't provide. Like select, drop returns a new table.", variabilization: {}}, ]; export {hints};