var hints = [{id: "farmers1c-h1", type: "hint", dependencies: [], title: "select function", text: "In such situations, we can use the table method select to choose only the columns that we want in a particular table. It takes any number of arguments. Each should be the name of a column in the table. It returns a new table with only those columns in it. The columns are in the order in which they were listed as arguments. For example, the value of farmers_markets.select(\"MarketName\", \"State\") is a table with only the name and the state of each farmers' market in farmers_markets.", variabilization: {}}, {id: "farmers1c-h2", type: "hint", dependencies: [], title: "careful", text: "Make sure to be exact when using column names with select; double-check capitalization!", variabilization: {}}, ]; export {hints};