import steps from './slope2-index.js'

const problem = {
  id: 'slope2', //Substeps will be in the form problem.id + 'a' and so on
  title: "Determining the slope of a graph",
  body: "",
  steps: steps
};
export { problem };