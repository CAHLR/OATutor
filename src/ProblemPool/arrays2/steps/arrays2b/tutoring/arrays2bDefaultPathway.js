var hints = [{id: "arrays2b-h1", type: "hint", dependencies: [], title: "join", text: "Strings have a method called join. join takes one argument, an array of strings. It returns a single string. Specifically, the value of a_string.join(an_array) is a single string that's the concatenation (\"putting together\") of all the strings in an_array, except a_string is inserted in between each string.", variabilization: {}}, ]; export {hints};