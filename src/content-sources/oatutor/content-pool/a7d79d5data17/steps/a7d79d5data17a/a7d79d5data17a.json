{
    "id": "a7d79d5data17a",
    "stepAnswer": [
        "select the range, drag the fill handle to the range you want to fill, then release the mouse button to generate the percentage series"
    ],
    "problemType": "MultipleChoice",
    "stepTitle": "How do you create a percentage series using the fill handle?",
    "stepBody": "",
    "answerType": "string",
    "variabilization": {},
    "choices": [
        "select the range, drag the fill handle to the range you want to fill, then release the mouse button to generate the percentage series",
        "write the first value and it will generate",
        "select the range, drag the fill handle to the range you want to fill, then click \"generate new percentage series\""
    ]
}